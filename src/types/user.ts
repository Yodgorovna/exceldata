export interface IUser {
    firstName: string;
    lastName: string;
    phoneNumber: string;
    birthDate: string;
    passportSeriaNumber: string;
    region: string;
    district: string;
    address: string;
    createdAt: string;
    updatedAt: string;
    id: number;
}